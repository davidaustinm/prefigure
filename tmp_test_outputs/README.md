# tmp_test_outputs

Scratch directory for **ephemeral test and build output**. The test suite (and
`prefig build` when invoked by the CLI smoke test) writes here so nothing stray
lands elsewhere in the tree.

Everything in this directory is gitignored except this README — do not commit
anything here, and feel free to delete its contents at any time.
